package com.souvanik;

public interface CLLInterface {

     int getLength(Node head);
     void printList(Node head);

     Node addFirst(Node head, int data);
     Node addLast(Node head, int data);
}
